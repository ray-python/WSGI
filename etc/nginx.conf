server {
    listen 80;
    proxy_set_header Host $host;
    proxy_set_header X-Real_IP $remote_addr;
	location ^~ /uploads/ {
		root /home/box/web/;
	}
	location ^~ /hello/ {
		proxy_pass http://0.0.0.0:8080/;
	}
	location / {
		return 404;
	}
}